const fs = require('fs');
const input = fs.readFileSync('/dev/stdin').toString().trim().split('\n');
[n, ...arr] = input;
//n은 arr의 보통 길이 arr은 숫자들 입력받을 때

function solution(n, arr) {
  const answer = [];
  return answer.join('\n');
}

console.log(solution(n, arr));